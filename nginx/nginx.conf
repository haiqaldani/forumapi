limit_req_zone $binary_remote_addr zone=one:10m rate=90r/m;

server {
    if ($host = www.rare-moons-shop-rapidly.a276.dcdg.xyz) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    if ($host = rare-moons-shop-rapidly.a276.dcdg.xyz) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    listen 80 default_server;
    listen [::]:80 default_server;

    server_name rare-moons-shop-rapidly.a276.dcdg.xyz www.rare-moons-shop-rapidly.a276.dcdg.xyz;
    return 404; # managed by Certbot

}

server {

    listen 443 ssl http2;
    server_name rare-moons-shop-rapidly.a276.dcdg.xyz www.rare-moons-shop-rapidly.a276.dcdg.xyz;

    listen [::]:443 ssl ipv6only=on; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/rare-moons-shop-rapidly.a276.dcdg.xyz/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/rare-moons-shop-rapidly.a276.dcdg.xyz/privkey.pem;

    # Rate limited /threads endpoint
    location /threads {
        limit_req zone=threads burst=10 nodelay;

        proxy_pass http://18.142.50.121/threads;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        limit_req zone=one;
    }

    # General API endpoints
    location / {
        proxy_pass http://18.142.50.121;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # Deny access to sensitive hidden files
    location ~ /\.(env|git|htaccess|htpasswd|ini|phps|fla|psd|log|sh)$ {
        deny all;
        return 404;
    }

    # Custom error pages
    error_page 404 /404.html;
    error_page 429 /429.html;
    error_page 500 502 503 504 /50x.html;

    location = /404.html {
        root /usr/share/nginx/html;
        internal;
    }

    location = /429.html {
        root /usr/share/nginx/html;
        internal;
    }

    location = /50x.html {
        root /usr/share/nginx/html;
        internal;
    }
}